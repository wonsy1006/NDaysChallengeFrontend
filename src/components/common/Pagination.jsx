import React from 'react';
import styled from 'styled-components';
import { RowWrapper, ColumnWrapper } from './Wrapper';

const Pagination = (props) => {
  return (
    <RowWrapper>
      <Button />
    </RowWrapper>
  );
};

export default Pagination;
